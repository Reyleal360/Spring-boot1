# Configuración general de la aplicación
spring.application.name=eventos-api

# Perfil activo (cambiar a 'test' para testing)
spring.profiles.active=dev

# Configuración de MySQL (por ahora comentada, usaremos memoria)
# spring.datasource.url=jdbc:mysql://localhost:3306/tiquetera_db
# spring.datasource.username=root
# spring.datasource.password=1234
# spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA/Hibernate (por ahora en memoria, sin persistencia)
# spring.jpa.hibernate.ddl-auto=update
# spring.jpa.show-sql=true
# spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# Configuración de Swagger/OpenAPI
springdoc.api-docs.path=/api-docs
springdoc.swagger-ui.path=/swagger-ui.html
springdoc.swagger-ui.enabled=true

# Puerto del servidor
server.port=8080